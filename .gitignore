# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.venv/
.uv/

# Test / coverage
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Jupyter executed notebooks
*.out.ipynb

# Node (legacy ESLint setup, no longer used)
node_modules/
package-lock.json

# macOS
.DS_Store

# Spacemacs (deleted; secret was committed — rotate Google Calendar OAuth token)
.spacemacs

# Personal overrides — never commit
.extra
.extrarc
.gitconfig.local

# Local project memories
.agents/memory/

# Editor/agent runtime state that lands here when tools run with this cwd
/.vim/
/.claude/worktrees/
/.claude/settings.local.json

# Written by `git config --global` while $HOME pointed here; the managed
# credential config now lives in ~/.gitconfig.profile instead.
/.gitconfig
